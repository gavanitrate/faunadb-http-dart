name: faunadb_http
version: 0.3.0
description: A basic communication library for interacting with FaunaDB using FQL in raw JSON
homepage: https://github.com/gavanitrate/faunadb-http-dart

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  http: ^0.12.1
  json_annotation: ^3.0.1

dev_dependencies:
  json_serializable: ^3.3.0
  build_runner: ^1.10.0